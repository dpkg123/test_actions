name: build cli

on:

  workflow_dispatch:
  push:

jobs:
  build-rootfs:
    runs-on: ubuntu-20.04
    permissions:
      contents: read
    steps:

    #- name: clean
    #  uses: rokibhasansagar/slimhub_actions@main
        
    - name: "Checkout Repo"
      uses: actions/checkout@v4
      with: 
        repository: Frain-Breeze/teatimetools
        fetch-depth: 2
        submodules: true
        submodule: true
    - name: Install package
      run: sudo apt install libxml2-dev libbz2-dev libssl-dev zlib1g-dev gcc g++ binutils ninja-build libzstd-dev
            
    - name: Checkout build files
      run: git submodule update --init

    - name: configure
      run: |
        mkdir out
        cmake . -GNinja -DCMAKE_INSTALL_PREFIX=out
    - name: Build
      run: |        
        ninja install
    - uses: actions/upload-artifact@v4
      with:
        name: out
        path: out
