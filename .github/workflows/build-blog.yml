name: deploy blog

on:

 workflow_dispatch:
 push:

jobs:

  deploy-blog: 
     runs-on: ubuntu-latest 
     steps:
       - run: docker pull registry.cn-hangzhou.aliyuncs.com/kernel_builder-arm64/kernel-builder:20.04
       - run: docker save -o test.tar.xz kernel-builder:20.04
