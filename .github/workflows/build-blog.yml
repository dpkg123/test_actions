name: deploy blog

on:

 workflow_dispatch:

jobs:

  deploy-blog: 
     runs-on: ubuntu-latest 
     steps:
     - name: Checkout Repo
       run: |
         wget https://github.com/dabao1955/android_kernel_OPPO_OP4ED5/archive/7fa40d154eae8be451e7f8efb8ff959538c23c10.tar.gz -O 1.tar.gz
         tar -xzf 1.tar.gz
         mv -v android_kernel_OPPO_OP4ED5* kernel-4.14
